package Clases;

public abstract class Animal extends SerVivo {

    // esta clase extiende de ser vivo por lo que si no se especifica que es abstracta,
    // se deberia proporcionar una implementacion (darle un cuerpo) al metodo abstracto de la clase SerVivo

    // para este caso, la clase Animal no tiene una definicion para el metodo abstracto alimentarse, por lo que
    // esta clase continua siendo abstracta
}
